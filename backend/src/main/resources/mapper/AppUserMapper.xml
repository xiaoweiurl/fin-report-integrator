<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cairui.finreport.user.AppUserRepository">
    <select id="findByUsername" resultType="com.cairui.finreport.user.AppUser">
        SELECT id, username, password_hash, display_name, role, created_at
        FROM app_users WHERE username = #{username}
    </select>
    <select id="count" resultType="long">SELECT COUNT(*) FROM app_users</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_users (username, password_hash, display_name, role, created_at)
        VALUES (#{username}, #{passwordHash}, #{displayName}, #{role}, COALESCE(#{createdAt}, NOW()))
    </insert>
</mapper>
